# v0.4 — Memory Architecture Update

**Status:** DESIGN. Not yet implemented. Drafted 2026-05-13.

**Companion to** [`MEM_FUSION_ARCHITECTURE.md`](MEM_FUSION_ARCHITECTURE.md) and [`CONSTELLATION_ARCHITECTURE.md`](CONSTELLATION_ARCHITECTURE.md). Once v0.4 ships, the load-bearing sections of those documents get amended to match this design and this file gets archived.

**Branch / tag context:** v0.3 line is cut at tag `v0.3.1` with maintenance branch `v0.3.x`. v0.4 work happens on `main`.

---

## 1. Why v0.4

v0.3 shipped a working group-memory system but left a real privacy gap: the dual-routing logic in `/remember` was a Band-Aid over a deeper architectural problem. The problem:

> v0.3 assumed Mem-Fusion could *classify* what's safe to share with teammates by looking at the content (rule vs. knowledge). But classification is inherently fuzzy, and any mis-classification on the wrong side becomes a privacy leak — a personal preference auto-pushed to your teammates' machines.

v0.4 replaces *content classification* with *explicit user intent*. Every memory has a `group` tag that the user sets (explicitly or by default). The group is the routing key for sharing. There is no fuzzy heuristic — if a memory isn't tagged for a group with peers, it doesn't leave the local machine.

This also dissolves the v0.3 separation between "local" and "federation" entries. There is one kind of entry now, distinguished only by which group it belongs to.

---

## 2. The new model in one sentence

**Every memory has a group tag. Sharing happens by group. Default group is `personal`. Same protocol, same storage, same dedup — only the peer list differs per group.**

---

## 3. Object model

### Memory entry

A Qdrant point with:

| Field | Purpose |
|---|---|
| `content` | The memory text, verbatim |
| `vector` | 768-dim embedding (nomic-embed-text) |
| `content_hash` | Dedup + integrity key |
| `group` | **Routing key for sharing.** Default `"personal"`. Set explicitly per-memory by the user. |
| `type` | Decorative filter (`decision` / `preference` / `fact` / `error` / `code` / `context` / `session`). Does NOT gate sharing. |
| `timestamp` | When this entry was stored locally |
| `origin_node` | Which peer originated this memory (= self for locally-originated) |
| `submitted_at` | When the memory was promoted to its group (= timestamp for locally-originated; preserved across pulls) |
| `importance` | 1–5, as today |
| `project`, `tags`, `session_id` | As today |

Notable absence: the `source` field (`local` / `group`) goes away. The `group` field replaces its routing role; `origin_node` covers the provenance role.

### Group

A named scope identifying who can see a memory. Examples: `personal`, `engineering@branch`, `LegalRO`.

- Every memory belongs to **exactly one** group.
- A peer can belong to **multiple** groups (multi-membership — relaxes the v0.3 single-membership limit).
- The default group is `personal`. New installs of Mem-Fusion + Constellation get a `personal` membership pre-configured with empty peers.

### "Personal" group — the implicit default

`personal` is a real group, not a sentinel for "local". Its peers list:

- **Empty** = single-machine personal memory. Memories tagged `personal` never leave this machine.
- **Populated** = your-own-machines. Memories tagged `personal` propagate across machines you own.

This is the load-bearing simplification of v0.4: there's no separate "local-only" code path. A truly-local memory is just one tagged `personal` whose group's peer list is empty.

---

## 4. /remember skill — the new shape

```
/remember <content>                       → store with group=personal
/remember <content> for <group>           → store with group=<group>
/remember push                            → for each group with peers, push that
                                              group's memories to its peers
/remember push <group>                    → push only that group's memories to its peers
/remember pull                            → for each group with peers, pull from
                                              that group's peers; merge into local store
/remember pull <group>                    → pull only that group's peers
```

The v0.3 classify-and-route logic is gone. Routing is now entirely explicit via the `for <group>` clause. Default = `personal` (local).

**Examples:**

```
You:   /remember always use uv for Python environments
Claude: ✓ Stored with group=personal. Stays on this machine.

You:   /remember We picked Postgres 16 for the auth service, for engineering@branch
Claude: ✓ Stored with group=engineering@branch.
        ✓ Pushed to engineering@branch peers:
            - bob-mac:       stored
            - alice-desktop: stored
```

A few notes:
- `for <group>` is the explicit-group syntax. The user has to opt-in to sharing. There is no auto-sharing.
- `/remember push` (without a group) iterates every configured group with non-empty peers and pushes that group's memories. Personal group with empty peers skips silently.
- `type=preference` continues to exist as a filter (search/recall semantics) but doesn't gate sharing. A `type=preference` memory in `group=engineering@branch` would share with engineering teammates; the user opted into that by tagging the group explicitly.

---

## 5. Storage model — dual write

Every memory is written to **both** the Qdrant vector DB **and** the file-based memory tree. The vector DB is the source of truth for semantic search; the file-based mirror is a resilience layer.

### Why both

1. **Search** — Qdrant gives semantic similarity over 768-dim vectors. Files don't.
2. **Resilience** — If Qdrant corrupts or Mem-Fusion is uninstalled, the files remain in `~/.claude/projects/<encoded-cwd>/memory/`. Claude Code loads them at session start. Claude keeps working — just without semantic search.

### File layout

```
~/.claude/projects/<encoded-cwd>/memory/
├── MEMORY.md                          # top-level index, grouped by group
├── personal/
│   ├── <slug>.md                      # one file per memory in this group
│   └── ...
├── engineering@branch/
│   ├── <slug>.md
│   └── ...
└── LegalRO/
    └── <slug>.md
```

Subdirectories per group. This makes it cheap to:
- Delete a whole group's local cache with `rm -rf memory/<group>/`
- See which memories belong to which group at a glance
- Add a new group without renaming any existing files

### File format

Per-memory `.md` file:

```markdown
---
name: <short human-readable name>
description: <one-line summary>
type: <decision | preference | fact | error | code | context | session>
group: <group name>
created_at: <ISO-8601 UTC>
qdrant_id: <UUID — pointer to the vector DB row>
---
<content body>
```

The `qdrant_id` is a back-reference. If Qdrant goes down, the file is still readable. If Qdrant comes back up, the vector DB entry can be reconstructed from the file (re-embed via Ollama).

### MEMORY.md index

Top-level index with one section per group:

```markdown
# Memory Index

## personal
- [Use uv for Python](personal/use-uv-for-python.md) — Always use uv for Python environments on this machine.
- [Concise responses](personal/concise-responses.md) — I prefer concise replies.

## engineering@branch
- [gRPC for internal RPC](engineering@branch/grpc-for-internal-rpc.md) — Use gRPC for service-to-service; HTTP/JSON for public APIs.
- [Postgres 16 + LR for auth](engineering@branch/postgres-16-lr-auth.md) — We picked Postgres 16 with logical replication for the auth service.
```

Loaded at session start by Claude Code's existing auto-memory mechanism.

### Write path

`core.store_memory(...)` becomes a single entry point that:

1. Computes `content_hash`. Dedup check on `(content_hash, group)` — skip if exists.
2. Embeds via Ollama if not a duplicate.
3. Writes to Qdrant with payload including `group`, `type`, etc.
4. Writes the corresponding `.md` file in `<memory_dir>/<group>/<slug>.md`.
5. Updates MEMORY.md to add a pointer.
6. Returns `{status: "stored", qdrant_id, file_path}`.

If file-write fails but Qdrant succeeds: log a warning, return success (Qdrant is the source of truth). If Qdrant fails but file write succeeds: log a warning, return `{status: "stored_to_file_only"}` so caller knows search won't reach it until vector DB recovers.

---

## 6. Sharing model — group-keyed

### Push

`/remember push [group]` triggers `group_push` on the Constellation gateway:

1. Daemon reads its config — finds all `memberships[]` entries with non-empty `peers[]`.
2. If a specific group was requested: only that membership.
3. For each such membership, scrolls local Qdrant for entries with matching `group` payload that haven't been pushed yet (or all — see open question below).
4. Fans out `POST /memory/put` to each peer with the full record.
5. Returns per-peer-per-group telemetry.

### Pull

`/remember pull [group]` triggers `group_pull`:

1. For each membership (or just the named one), iterate peers.
2. Call `GET /memory/since?group=<group>&cursor=<max submitted_at in this group>` on each peer.
3. Insert returned entries with their group tag preserved (dedup on `content_hash + group`).
4. Returns per-peer-per-group telemetry.

### What's NOT in this protocol

- **No cross-group propagation.** A memory in `engineering@branch` doesn't bleed into `personal` even if both groups share peers. Group is the scope.
- **No automatic re-grouping at receive time.** Receiver stores the entry with the originator's group tag, preserved verbatim.
- **No content classification.** Routing is by tag, not by inspection.

---

## 7. Constellation alignment

### Multi-membership

v0.3 restricted `memberships` to length 1. v0.4 lifts that. Config:

```json
{
  "node_name": "alice-mac",
  "peer_listen_address":    "0.0.0.0:7533",
  "gateway_listen_address": "127.0.0.1:7534",
  "qdrant_url":             "http://127.0.0.1:6333",
  "state_dir":              "~/.local/share/mem-fusion/constellation",
  "memberships": [
    {
      "group_name": "personal",
      "peers": [
        { "node_name": "alice-desktop", "endpoint": "http://10.0.0.4:7533" }
      ]
    },
    {
      "group_name": "engineering@branch",
      "peers": [
        { "node_name": "bob-mac",      "endpoint": "http://10.0.0.5:7533" },
        { "node_name": "carol-laptop", "endpoint": "http://10.0.0.7:7533" }
      ]
    }
  ]
}
```

The `personal` membership is pre-populated by the installer with `peers: []`. Cross-machine personal sync is opt-in: the user adds their other machines to the personal group's peer list when they're ready.

### Out-of-the-box personal-group experience

After installing Constellation on each of your machines and adding each other to the personal group's peers list, your memories sync automatically across your devices. Same protocol as teammate-group sharing, just a different peer list.

---

## 8. Backward compatibility with v0.3 data

Existing v0.3 vector DB entries lack a `group` payload field (some were `source=local` without group; others were `source=group` with `group_name`).

**Read-time fallback rule:** any entry without a `group` field is treated as `group=personal`. No mandatory data migration. The v0.3 `source=group` entries with `group_name` already have the right shape (rename `group_name` → `group` at read time, or run a one-time index migration).

`v0.3 → v0.4 install` script proposed (optional, opt-in):
- Iterate all entries lacking `group` payload.
- For entries with `source=group` and `group_name=X`, write `group=X`.
- For entries without `source=group`, write `group=personal`.
- Idempotent; can be re-run.

---

## 9. What stays the same from v0.3

The structural pieces below don't change; v0.4 is a re-routing inside the same skeleton:

- **Two-listener Constellation daemon** (peer surface + gateway surface) — same code shape, same ports.
- **`core.py` shared library** — same module surface, `store_memory` gets the `group` arg added but otherwise unchanged.
- **`mem_fusion.py` MCP server** — 11 tools today; `group_pull` / `group_push` adapt to group-keyed routing, signatures unchanged.
- **Test harness** (`tests/lib/harness.py`) — same lifecycle helpers.
- **Self-contained test discipline** — every test still spins up its own Qdrant + daemons; nothing depends on externally-running infrastructure.
- **The four hooks** — `SessionStart`, `UserPromptSubmit`, `Stop`, `PostToolUse:Write`. The Stop hook's extraction logic stays; what changes is that every hook-captured entry gets `group=personal` by default. Routing-by-type goes away.

---

## 10. v0.4 work packages (high-level ordering)

| WP | Body of work | Depends on |
|---|---|---|
| WP1 | `core.store_memory` adds `group` field (default `"personal"`); dedup includes group | — |
| WP2 | `core.py` writes per-memory file alongside the Qdrant entry; MEMORY.md index aggregation; recovery semantics if either store fails | WP1 |
| WP3 | `constellation.py` accepts multi-membership config; `/pull` and `/push` accept optional group filter | WP1 |
| WP4 | `mem_fusion.py` group tools route by group; new `/remember <content> for <group>` syntax | WP1, WP3 |
| WP5 | Rewrite `/remember` skill for v0.4 routing | WP4 |
| WP6 | Update all hooks (`Stop`, `PostToolUse:Write`) to default `group=personal` (no classification) | WP1 |
| WP7 | Backward-compat read-time fallback for v0.3 entries (`group` missing → treat as `personal`) | WP1 |
| WP8 | Test suite update — self-contained tests cover multi-membership, group-keyed push/pull, dual-write resilience | All above |
| WP9 | Docs: architecture doc updates, README rewrite, INSTALL prose updates, CHANGELOG entry | All above |

---

## 11. Open questions still TBD

- **Push idempotency.** Today's push sends the same memory every time it's pushed. v0.4 could track per-(peer, group, memory) push state to skip already-delivered entries. Or accept the redundancy (the receiver dedups). MVP: accept the redundancy.
- **Group-rename ergonomics.** What happens if a user wants to rename `engineering@branch` to `eng@branch`? Probably: rename in config + rename memory subdirectory + bulk-update Qdrant payload. Out of scope for v0.4; document as a future operational task.
- **What about memories the user wants to share but already stored as personal?** Today's `/remember <content> for <group>` works at store time. A separate `/promote <id> to <group>` command would be useful for re-grouping existing memories. Out of scope for v0.4 unless real use surfaces it.
- **Hook-extracted memory types.** Today's Stop hook extracts `preference`, `decision`, `error`. Under v0.4, all of these store with `group=personal` by default — including preferences (which previously were the privacy-leak risk). Group tag handles the safety. Worth re-examining whether the hook's pattern extractors are still well-calibrated; might be over-eager for v0.4's clearer semantics.

---

## 12. What this dissolves vs. what it preserves

**Dissolves:**
- The dual-routing skill logic (classify rule vs. knowledge)
- The `source=local` vs `source=group` distinction
- The "/CLAUDE.md is hand-edited but vector DB is automatic" two-mode user mental model
- The single-membership constraint on Constellation
- The need for hook-extracted preferences to route differently from hook-extracted decisions

**Preserves:**
- All test infrastructure
- The protocol surfaces (peer + gateway HTTP, MCP tools)
- The dedup-by-content-hash invariant
- Verbatim vector preservation across the wire
- Self-contained test discipline
- The two-process model (Mem-Fusion stdio MCP + Constellation HTTP backend)

**Adds:**
- File-based dual-write for resilience
- Multi-group membership per peer
- Group-keyed sharing as the routing primitive
- The `personal` group's special role as the unified default

---

*See [`CHANGELOG.md`](../CHANGELOG.md) for the v0.3 line; this doc is the v0.4 design baseline. Implementation work tracked on `main` branch off tag `v0.3.1`.*
